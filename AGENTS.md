For additional project context, review OpenSpec source-of-truth specs:
- openspec/specs/llama-cpp-snap/spec.md
- openspec/specs/generate-default-presets/spec.md

## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues for this repository (via `gh`). See `docs/agents/issue-tracker.md`.

### Triage labels

Triage uses the default five labels: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Domain docs are single-context: root `CONTEXT.md` and root `docs/adr/`. See `docs/agents/domain.md`.
